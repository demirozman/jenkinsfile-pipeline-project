pipeline {
    agent any {
        stages ('build') {
            echo "My Analog Clock"
            sh 'echo using shell within Jenkinsfile'
            echo "nit using shell in the Jenkinsfile"
        }
    }
}